if (messageE.length != 0)
    .alert.alert-dismissable.alert-danger.fade.in #{messageE}
        button.btn.btn-negative.btn-outlined(type="button", class="close", data-dismiss="alert") &times;
if (messageI.length != 0)
    .alert.alert-dismissable.alert-info.fade.in #{messageI}
        button.btn.btn-primary.btn-outlined(type="button", class="close", data-dismiss="alert") &times;
if (messageS.length != 0)
    .alert.alert-dismissable.alert-success.fade.in #{messageS}
        button.btn.btn-positive.btn-outlined(type="button", class="close", data-dismiss="alert") &times;